from django.contrib import admin

# Register your models here.


# class EnteryAdmin(admin.ModelAdmin):
# 	list_display = ('title', 'created')
# 	prepopulated_fields = {'slug': ('title',)}

# admin.site.register(models.Entry, EnteryAdmin)